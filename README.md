# PDF Compress

## Requirements

uv and Task are required

```bash
brew install uv go-task
```

## Running

Add pdf to `./files` directory, named `input.pdf`

edit `compress.py` to change the `DPI` and `JPG quality` to the values you desire, then run

```bash
task run
```
